# Homebrew Tap

Install Teams Mute Helper:

```sh
brew install --cask m-rk/tap/teams-mute-helper
```

The app's source, releases, and setup guide are in [`m-rk/ms-teams-mute-shortcut`](https://github.com/m-rk/ms-teams-mute-shortcut).
